services:
  jarvis-sync:
    build: .
    ports:
      - "8787:8787"
    environment:
      JARVIS_SYNC_BIND: "0.0.0.0:8787"
      DATABASE_URL: "sqlite:/data/jarvis-sync.db"
    volumes:
      - jarvis-sync-data:/data
    restart: unless-stopped

volumes:
  jarvis-sync-data:
